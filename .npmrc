legacy-peer-deps=false
strict-peer-deps=false

# Skip Puppeteer Chromium download (whatsapp-web.js dependency)
# This prevents issues in CI/CD environments where Chromium download may fail
# Playwright is used for E2E testing instead
puppeteer_skip_download=true
